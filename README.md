# Basic Graphics 3D Rotating Triangle Models

Submission examples for a basic graphics coding test.

Each sample renders a rotating 3D triangular model, using a tetrahedron so the shape has triangular faces and visible depth.

## 1. OpenGL

Folder: `1_openGL`

```powershell
cd "C:\Users\USER\새 폴더\graphics_test\1_openGL"
.\build.bat
.\rotating_triangle_opengl.exe
```

## 2. DirectX 12

Folder: `2_DirectX12`

```powershell
cd "C:\Users\USER\새 폴더\graphics_test\2_DirectX12"
.\build.bat
.\rotating_triangle_directx12.exe
```

## 3. three.js

Folder: `3_Three.js`

```powershell
cd "C:\Users\USER\새 폴더\graphics_test\3_Three.js"
npm.cmd install
npm.cmd run dev
```

Open:

```text
http://127.0.0.1:5303/
```

## 4. React Three Fiber

Folder: `4_r3f`

Run:

```powershell
cd "C:\Users\USER\새 폴더\graphics_test\4_r3f"
npm.cmd install
npm.cmd run dev
```

Open:

```text
http://127.0.0.1:5304/
```
